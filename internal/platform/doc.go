// Package platform owns bounded host integration outside filesystem traversal.
//
// It discovers distinct mounted volumes and familiar existing scan locations,
// reads capacity for an explicitly selected volume root, and reveals
// already-authorised scan nodes with fixed operating-system commands. Target
// discovery may inspect metadata and a bounded, non-recursive two-level sample
// in known user and cloud-provider locations. The package does not accept browser
// commands, execute through a shell, scan filesystems, or own HTTP validation.
package platform
